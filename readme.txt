1. run npm install
	la commande va downloader et installer tout les modules necessaires dans le package.json
2. executer la commande build.bat, c'est un fichier batch qui compile les fichier TS et copie tous les fichier
	HTML, CSS, IMG.
3. executer le code dans le dossier DIST, pas SRC (sinon rien ne fonctionnera)


BUGS:
--Je ne sais pas ou implementer les Events de click de boutton
	--Peut etre une fonction d'init qui a acces a tous les fichier HTML ??